/**
 * Repo-saved module configuration
 */
module.exports = {
    // Load module classes and services, path names
    autoload: [
        'src',
    ],

    // Translation directories
    i18n: [
        'i18n',
    ],

    // View directories
    views: [
        'views',
    ],

    // Static directories
    static: [
        'public',
    ]
};
