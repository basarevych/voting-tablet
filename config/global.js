/**
 * Repo-saved application configuration
 */
module.exports = {
    // Project name (alphanumeric)
    project: 'votingtablet',

    // Load base classes and services, path names
    autoload: [
        '~arpen/src',
        'src',
    ],
};
